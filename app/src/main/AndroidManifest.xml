<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.me.noban.hiproject">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Chat.Chating" />
        <activity android:name=".BloodBank.bloodbankHome" />
        <activity android:name=".chat.Chating" />
        <activity android:name=".maps.MapsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pulseRate.pulsrateMain" />
        <activity android:name=".pulseRate.pulsMeasure" />
        <activity android:name=".userProfile.userProfile" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".navigation.navigationActivity" />
        <activity android:name=".PatientSignUpActivity" />
        <activity android:name=".userProfile.DoctorSignUpActivity" />
        <activity android:name=".DoctorHomeActivity" />
        <activity
            android:name=".MainUIActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".checkApointmentDoctor.Activity.CheckApointment" />
        <activity android:name=".checkApointmentDoctor.Activity.check_apointment" />
        <activity android:name=".checkApointmentDoctor.check_apointment"></activity>
    </application>

</manifest>